version = "1.0.0-SNAPSHOT"

apply plugin: 'java'

task jasmine(type: Exec) {
  commandLine 'npm', 'test'
}

task archive(type: Zip) {
  from ('./src')
  baseName = "resources"
  dependsOn jasmine
}

task build() {
  dependsOn jasmine, archive
}
