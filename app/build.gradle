version = "1.0.0-SNAPSHOT"

task jasmine(type: Exec) {
  commandLine 'npm', 'install-test'
}

task archive(type: Zip) {
  from ('./src')
  baseName = "app"
  dependsOn jasmine
}

task build() {
  dependsOn jasmine, archive
}
