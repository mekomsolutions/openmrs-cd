task npmBuild(type: Exec) {
  commandLine 'npm', 'run', 'all'
}

task copyScripts(type: Copy) {
  from ("./src")
  into ("$rootProject.buildDir/openmrs-cd/node-scripts")
  dependsOn npmBuild
}

task build() {
  dependsOn npmBuild, copyScripts
}
