task gitCleanup() {
  ['sh', '-c', 'git clean -Xdf'].execute()
}

task copyJenkinsHome(type: Copy) {
  from ("./jenkins_home")
  into ("$rootProject.buildDir/openmrs-cd/jenkins_home/")
}

task build() {
  dependsOn gitCleanup, copyJenkinsHome
}